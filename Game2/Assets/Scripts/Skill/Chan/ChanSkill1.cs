using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChanSkill1 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(this.gameObject, 0.49f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
