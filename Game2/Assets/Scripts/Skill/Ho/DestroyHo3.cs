using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyHo3 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(this.gameObject, 0.4f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
